package com.sample.composenav3.ui.auth.register

import androidx.compose.runtime.Composable

@Composable
fun RegisterScreen(
    onNavigateBack: () -> Unit,
    onNavigateToMain: () -> Unit
) {
    // Register UI - navigation logic only
    // Call onNavigateBack() to go back to login
    // Call onNavigateToMain() after successful registration
}
