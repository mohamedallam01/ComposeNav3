package com.sample.composenav3.ui.main.details

import androidx.compose.runtime.Composable

@Composable
fun DetailsScreen(
    itemId: String,
    onNavigateBack: () -> Unit
) {
    // Details UI - navigation logic only
    // itemId is the ID of the item to display
    // Call onNavigateBack() to go back
}
